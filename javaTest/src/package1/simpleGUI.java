package package1;
/*import java.awt.Color;
import javax.swing.ImageIcon;
import javax.swing.JFrame;*/

public class simpleGUI {

	public static void main(String[] args) {
		/*
		JFrame frame = new JFrame(); //creates a frame
		
		frame.setTitle("Cool Title"); // sets the window title
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit the application
		frame.setSize(420,420); //sets the dimension of frame
		frame.setResizable(false); //prevents from being resized
		frame.setVisible(true); //make the frame visible
		
		ImageIcon image = new ImageIcon("logo.png");
		frame.setIconImage(image.getImage()); //sets the icon of the windows
		frame.getContentPane().setBackground(new Color(255,255,32)); // change the background color
		//frame.getContentPane().setBackground(new Color(0xFFFFFF)); hexadecimal colors
	*/
		
		new myFrame();
	}


}
